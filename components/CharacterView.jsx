import React from 'react';

    const CharacterView = () => {
      return (
        <div>
          <h1>Character View</h1>
          {/* Placeholder for character profiles */}
        </div>
      );
    };

    export default CharacterView;
